# Contributing

- Branches: `main`, `feat/<name>`, `fix/<name>`
- Commits: `feat: ...`, `fix: ...`, `chore: ...`

## Flow
```bash
git checkout -b feat/your-feature
git add -A
git commit -m "feat: your feature"
git push -u origin feat/your-feature
```
Open a Pull Request.
